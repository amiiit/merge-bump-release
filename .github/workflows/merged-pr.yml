name: Merged PR to main branch

on:
  pull_request:
    types:
      - closed
      - synchronize
jobs:
  release:
    name: PR
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Run action
        uses: amiiit/merge-bump-release@v0.1
        with:
          foo: 'hello'